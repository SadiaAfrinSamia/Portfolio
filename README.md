# Sadia — UI/UX Internship Portfolio

Open `index.html` in a browser to view the responsive portfolio. No build steps required.

Quick preview (Windows PowerShell):

```powershell
start .\index.html
```

Files added:
- index.html — main page
- styles.css — styling and responsive layout
- script.js — small interactivity
